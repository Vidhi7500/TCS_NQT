#include<bits/stdc++.h>
using namespace std;

int main()
{
    string str[]={"break", "case", "continue", "default", "defer", "else", "for", "func", "goto", "if", "map", "range", "return", "struct", "type", "var"};
    string g ;
    cin>>g;
    int cnt=0;
    for(int i=0;i<16;i++){
        if(str[i]==g){
            cout<<g<<" is a keyword"<<endl;
            cnt++;
            break;
        }
    }
    if(cnt==0){
        cout<<g<<" is not a keyword"<<endl;
    }
    return 0;
}
